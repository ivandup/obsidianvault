When you have a .pcap file and you open it.
Have a look for any entries that might have values.
Right click in the TCP > Stream > Follow

This should give you a nice output to read instead of investigating the packets individually.